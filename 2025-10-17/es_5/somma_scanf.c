#include<stdio.h>
#include<stdlib.h>

int a, b, somma, prodotto;

int main(){
    scanf("%d" "%d", &a, &b);
    printf("la somma dei due numeri è %d", a + b);
    printf("il prodotto dei due numeri è %d", a * b);

    return 0;
}

