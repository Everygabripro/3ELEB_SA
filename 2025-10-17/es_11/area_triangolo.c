#include <stdio.h>

int base, altezza, area;

int main8(){
    scanf("inserisci %.2f", base);
    scanf("inserisci %.2f", altezza);

    area = (base * altezza)/2 ;

    printf("l'area è %.2f", area)

    return 0;
}